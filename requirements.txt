# Core pipeline (CPU-only, no GPU required)
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
SALib>=1.4          # Latin Hypercube / Sobol design of experiments
xgboost>=2.0        # gradient-boosted trees (falls back to sklearn GBT if absent)
PyYAML>=6.0
matplotlib>=3.7     # figures
seaborn>=0.12       # nicer plots (optional)

# Optional, for the heavier active-learning variant:
# gpytorch>=1.11
# botorch>=0.9
# torch (CPU build):  pip install torch --index-url https://download.pytorch.org/whl/cpu

# ParaEMT itself is a separate checkout (github.com/NREL/ParaEMT_public),
# not a pip package. See README "Wiring in ParaEMT".
